# Fichiers inutiles à la construction de l'image (image plus légère + build plus rapide)
.git
.github
node_modules
dist
.env
.env.local
*.md
docs

# Scripts de test contenant des identifiants (jamais dans l'image)
*.ps1

# Docker lui-même
Dockerfile
.dockerignore
docker-compose*.yml
